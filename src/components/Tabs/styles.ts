import { makeStyles } from "@material-ui/core";

const TabsStyles = makeStyles(() => ({
  root: {},
  tabsStyle: {
    marginBottom: "10px",
  },
  optionsWrapper: {
    margin: "30px 0px",
  },
}));

export default TabsStyles;
