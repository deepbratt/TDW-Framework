import { makeStyles } from "@material-ui/core";

const TabsStyles = makeStyles(() => ({
  root: {},
  tabsStyle: {
    marginBottom: "10px",
  },
  optionsWrapper: {
    margin: "30px 10px",
  },
}));

export default TabsStyles;
